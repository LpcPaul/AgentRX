# Legacy Architecture Context

These files document the v1 → v2.1 architecture exploration: schema validation, route registry, deterministic retrieval, hook automation, and case collection pipelines.

They are preserved for design decision reference, but **are not the current MVP implementation path**.

Current MVP: ultra-short SKILL.md + flat `cases/` + `ledger.jsonl`.
